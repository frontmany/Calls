#pragma once

namespace calls {

enum class Result {
	EMPTY,
	WRONG_FRIEND_NICKNAME,
	UNEXISTING_GROUP_CALL,
	JOIN_DECLINED,
	JOIN_ALLOWED,
	TIMEOUT,
	CALLING,
	UNAUTHORIZED,
	CALL_ACCEPTED,
	CALL_DECLINED,
	SUCCESS,
	FAIL,
	TAKEN_GROUP_CALL_NAME
};
}